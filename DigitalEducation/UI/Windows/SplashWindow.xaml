<Window x:Class="DigitalEducation.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Width="{Binding Source={x:Static SystemParameters.PrimaryScreenWidth}}"
        Height="{Binding Source={x:Static SystemParameters.PrimaryScreenHeight}}"
        Topmost="True"
        ShowInTaskbar="False">
    <Border Background="{DynamicResource SurfaceBrush}"
            CornerRadius="0">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30">
            <Border Width="100" Height="100" CornerRadius="20" 
                    Background="{DynamicResource PrimaryBrush}" 
                    Margin="0,0,0,25">
                <Image x:Name="LogoImage" Tag="Book" Width="50" Height="50"/>
            </Border>
            <TextBlock Text="DigitalEducation" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="Ваш помощник в цифровой грамотности" 
                       FontSize="16" 
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,50"/>
            <Image x:Name="LoaderImage" Tag="Loader" Width="40" Height="40" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <RotateTransform Angle="0"/>
                </Image.RenderTransform>
            </Image>
            <TextBlock Text="Загрузка..." 
                       FontSize="14" 
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"/>
        </StackPanel>
    </Border>
</Window>